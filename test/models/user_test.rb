require "test_helper"

describe User do
  describe 'validations' do
    # user = User.new(name: "Katie Kennedy", join_date: Time.now)
    # it 'can be valid' do
    #   is_valid = user.valid?
    #   expect(is_valid).must_be_true
    # end
  end
  
  
end
